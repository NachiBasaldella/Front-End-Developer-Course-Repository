//EJ 01
let edad = prompt(`edad`)
let tieneAutorización = prompt(`tiene autorización de padres?`)
let numero = 15

function puedeVerPelicula(a, b) {
    if (edad >= numero) {
        return true
    } else if (tieneAutorización === si) {
        return true
    } else {
        return false
    }
}

const resultado = puedeVerPelicula(a, any, b, any)
console.log(resultado)